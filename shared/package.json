{
  "name": "@pressd/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Press'd domain types, API client, and shared hooks — consumed as TypeScript source by both the web frontend (Vite) and the mobile app (Metro).",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./api": "./src/api.ts",
    "./albumSearch": "./src/albumSearch.ts",
    "./hooks/useAlbumSearch": "./src/hooks/useAlbumSearch.ts"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^19.2.14"
  }
}
